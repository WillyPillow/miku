#!/bin/bash

isolate-check-environment --execute
echo 10000 > /proc/sys/kernel/sched_rr_timeslice_ms

mkdir -p ./testdata
mkdir -p ./testzone
export PATH=$PATH:./app:./bin
miku --verbose --parallel 4 -b 10
